# TODO

1. Riccardo, take learnings from here: https://dev.to/uenyioha/writing-cli-tools-that-ai-agents-actually-want-to-use-39no esp the last Quick Reference checklist.
2. Also check: https://alies.dev/articles/cli-output-for-ai/ (useful but not too much unless you write PHP, shows flags to disable progress dots/verbosity).
3. Think of an `--ai-help` option to provide a customized help message specifically for AI agents. When an agent runs `--help`, it discovers `--ai-help` and runs it to receive structured instructions, integration constraints, and formatting rules.
