# Add this to your project's Makefile to monitor the GHI Fan-Out Bonanza!
show-fanout-execution:
	@if [ -z "$(UUID)" ]; then echo "Usage: make show-fanout-execution UUID=123"; exit 1; fi
	bash $SKILL_DIR/scripts/dashboard.sh $(UUID)
